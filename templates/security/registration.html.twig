{% extends "base.html.twig" %}


{% block body %}
    <div class="container">  
        <div class="row justify-content-md-center">
            <img src="/image/sigleSteg.png">
        </div>
        <div class="row justify-content-md-center">
            {{ form_start(inscrit) }}
                <div class="row">
                    <div class="col-md-4">
                        {{ form_row(inscrit.username,{'label': 'Utilisateur :' ,'attr':{'placeholder':'Walid...'}})}}
                        {{ form_row(inscrit.email, {'label': 'Email :','attr':{'placeholder':'walidnagga@gmail.com...'}}) }}
                        <div class="row justify-content-start">
                        <div class="col-12">
                            <button type="submit" class="btn btn-primary">S'inscrire</button>
                        </div>
                    </div>
                    </div>
                    <div class="col-md-4">
                        {{ form_row(inscrit.password,{'label':'Mot de passe :','attr':{'placeholder':'Mot de passe...'}}) }}
                        {{ form_row(inscrit.confirm_password,{'label':'Mot de passe :','attr':{'placeholder':'Mot de passe...'}}) }}
                    </div>   
                    <br>
                   
                </div>
             {{ form_end(inscrit) }}    
        </div>                     
           
    </div>

{% endblock %}

